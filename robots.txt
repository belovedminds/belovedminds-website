# The site is open to search engines. The four real pages — home, privacy,
# terms, and cancellation — carry no robots meta tag, so they index normally.
#
# The design variants (/plum/, /archive/clay/, /archive/heritage/,
# /archive/slate/) and /template.html each still carry
# <meta name="robots" content="noindex, nofollow">. They are near-identical
# copies of the homepage; letting them index would put duplicate content in
# front of Google and risk ranking a variant over the real front page.
# Leave those tags alone.
#
# Crawling stays ALLOWED for everything: a crawler has to fetch a page to
# read its noindex tag, so blocking here would hide the instruction and
# Google could still list the URL.
User-agent: *
Allow: /

Sitemap: https://belovedminds.care/sitemap.xml
